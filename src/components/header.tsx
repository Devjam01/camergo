export default function Header(): JSX.Element {
  return (
    // Définit le composant de l'en-tête
    <header className="header">
      {/* Élément d'en-tête avec une classe CSS "header" */}
      <h1>Réservez votre Voyage</h1>
      {/* Titre principal de l'en-tête */}
    </header>
  );
}
