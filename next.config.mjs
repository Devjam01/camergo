/* ce fichier JavaScript sert de point de départ pour configurer Next.js. 
La ligne de commentaire spéciale indique les attentes de type pour nextConfig, assurant ainsi la conformité avec les spécifications de Next.js. 
L'objet nextConfig peut être enrichi avec des options spécifiques à Next.js telles que la configuration du serveur, les options de routage, 
les paramètres de compilation, et plus encore, en fonction des besoins du projet.*/

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
